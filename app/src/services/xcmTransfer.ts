/**
 * Uses the AssetTransfer API by Parity to initiate a transfer between XCM-compatible chains.
 *
 * @throws An error if the transfer is not initiated successfully.
 */
export const initiateTransfer = async (): Promise<void> => {
  // TODO: implement this function using AT API and define its interface

  return
}
