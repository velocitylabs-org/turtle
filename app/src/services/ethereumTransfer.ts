/**
 * Uses the Snowbridge SDK to initiate a transfer from Ethereum to Assethub.
 *
 * @throws An error if the transfer is not initiated successfully.
 */
export const initiateTransferToAssetHub = async (): Promise<void> => {
  // TODO: implement this function using snowbridge

  return
}

/**
 * Uses the Snowbridge SDK to initiate a transfer from Assethub to Ethereum.
 *
 * @throws An error if the transfer is not initiated successfully.
 */
export const initiateTransferToEthereum = async (): Promise<void> => {
  // TODO: implement this function using snowbridge

  return
}
