interface CopyAddressProps {
  content: string
  address: string
  showIcon?: boolean
}
export default function CopyAddress({
  content,
  address,
  showIcon,
}: CopyAddressProps): import('react/jsx-runtime').JSX.Element
