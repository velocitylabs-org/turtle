const useSubstrateWalletAddress = () => {
  const getAddress = (): string | undefined => {
    // TODO: read address from app state
    return undefined
  }

  return { getAddress }
}

export default useSubstrateWalletAddress
